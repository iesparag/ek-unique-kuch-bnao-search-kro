# Build plan

1. Setup project structure, packages, base config, and README
2. Implement storage module with JSON read/write, corruption handling, and tests
3. Implement unique item generator creating UUID + random phrase, with tests
4. Integrate 'generate' CLI command end-to-end
5. Implement search module with substring case-insensitive matching, with tests
6. Integrate 'search' CLI command end-to-end
7. Implement 'list' and 'delete' CLI commands end-to-end
8. Add robust error handling, input validation, edge case handling across modules and CLI
9. Write integration tests simulating CLI command sequences
10. Final polishing: readme update, env var config, usage docs, and preparation for deployment
